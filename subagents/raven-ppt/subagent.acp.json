{
  "name": "Raven-PPT",
  "kind": "acp",
  "description": "A powerful deck-building agent that can turn source documents into a fact-grounded .pptx, reviewing every rendered page before it publishes. Use this agent for slides built from material you already have. Open the task with a fenced block naming the inputs by absolute path, then write the request below it:\n```raven-ppt\n{\"materials\": [\"/abs/notes.md\", \"/abs/figures.xlsx\"], \"template\": \"/abs/house.pptx\"}\n```\n`template` is optional and names the file the deck is built in, not evidence to quote. A path named only in prose is still picked up, but a quoted one survives spaces and non-ASCII punctuation, which prose does not. The finished deck is copied into the calling session's own workspace.",
  "owns": "owns decks and slides built from source documents. Do not build the deck yourself.",
  "enabled": true,
  "command": "{PYTHON} {SUBAGENT_DIR}/acp.py",
  "readyTimeoutMs": 180000,
  "timeout": 10800,
  "maxOutputChars": 60000,
  "everos": {
    "userId": "raven-ppt",
    "agentId": "raven-ppt"
  }
}
