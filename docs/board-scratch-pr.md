# Review board write-path check

This file exists only so the merge-review board can exercise its GitHub
write path once against a real pull request: file a review thread on this
file, reply into it, resolve it, and post a plain comment. The pull request
that adds it is closed unmerged afterwards.
