[project]
name = "zep-memory"
version = "0.1.0"
description = "Zep Cloud (getzep.com) as a Raven memory backend"
requires-python = ">=3.12"
license = "Apache-2.0"
authors = [
    {name = "EverMind"}
]
keywords = ["raven", "raven-plugin", "memory", "zep"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.12",
]

# No vendor SDK: the backend is a handful of REST calls over the httpx the
# host already carries, on the HttpMemoryBackend base the host provides.
dependencies = [
    "raven",
    "httpx>=0.28.0,<1.0.0",
]

[project.entry-points."raven.plugins"]
zep-memory = "raven_zep"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["raven_zep"]

# Discovery loads raven-plugin.toml out of the installed package; a wheel
# without it installs a plugin nothing can find.
[tool.hatch.build]
include = [
    "raven_zep/**/*.py",
    "raven_zep/raven-plugin.toml",
]

[tool.hatch.build.targets.sdist]
include = [
    "raven_zep/",
    "pyproject.toml",
]

[tool.uv.sources]
raven = { workspace = true }
