"""Raven-Design visual engine distribution.

The fork's product delta as one wheel on the ``raven.plugins`` entry-point
group: the rendering and preview lane (``raven_design.rendering`` +
``raven_design.tools.render``), the Visual Domain Selector
(``raven_design.selector``), the resident Task State
(``raven_design.task_state``), the packaged domain-Skill corpus with its
reference plates (``raven_design/skills``), and the plugin face that mounts
them (``raven_design.plugin`` + ``raven-plugin.toml``).
"""
