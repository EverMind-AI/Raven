# 品牌标杆索引（按档位读格，不通读）

规则：先在 `BRAND-BRIEF.md` 完成第零步档位翻译，再按下表读命中的格；两张页都只有一页，读页时只看命中格并把它
记进 `REFERENCE-CONTRACT`（读了哪一格、学了哪条关系）。全部为真实品牌成品（2025–26，来源 Shopify 品牌案例）。
只学结构关系，禁止复制任何一家的名称、颜色、字体、道具或标语。

## `brand-examples/page-1.jpg`（品牌示例：一帧像真的一样）

| 格 | 品牌 | 命中档位 | 学什么 |
| --- | --- | --- | --- |
| 左上 | Mount Lai | `basic` `full`（护肤 / 器具 / 安静气质） | 白底细衬线 + 玉石道具，一帧同时成立 B1 主色统治、B3 一种调子、B7 像真 |
| 右上 | Glossier | `packaging_kit` `social_kit` | 浅粉与字标落在瓶身，标志在真实载体上（B4）；同一色在包装与网页重复 |
| 左下 | Noted | `logo_kit` `basic`（手作 / 自然） | 木桩、松果与素标签：图像调子一句可述；标志极简仍能认出 |
| 右下 | Hero Bread | `packaging_kit` `full`（食品 / 货架） | 金色包装在大理石上：主色在货架近邻中的位置，标志在包装正面的比例 |

## `brand-kits/page-1.jpg`（品牌工具包：文件夹铺开是什么样）

| 格 | 品牌 | 命中档位 | 学什么 |
| --- | --- | --- | --- |
| 左上 | LUVEU | `basic` `full` | 同一套标志、色、字落在包装、名片、招牌、杯子的平铺；B5 可溯源 |
| 右上 | Spotify | `full`（数字为主） | 色点、标题字规格、图标集、照片处理排成一版；工具包的"每项有值" |
| 左下 | EcoAvenue | `packaging_kit` `full` | 平铺里同一世界的光线与材质；纸袋、盒、标签同一标志形状 |
| 右下 | Water Lily Pops | `social_kit` `packaging_kit` | 网站与包装并排一致：数字与实物两种载体一个世界 |

空着的格子（当前只有每档 1–2 例，未到每档四例）：`logo_kit` 的纯字标 / 图形标成品、`social_kit` 的头像—封面—帖子
三件套、`full` 的空间实景（门店 / 展位）。补齐前以判据 B1–B7 与共享图鉴兜底；来源见 `../manifest.json`（若有）。
