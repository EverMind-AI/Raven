"""The Raven-PPT deck engine's future import home.

This wave the package carries payload machinery only: the pinned template
manifest's destination directory (gitignored; see assets/templates/) and the
fork's three drifted prompt templates (prompts/). The engine itself -- the
fork's ``raven.ppt`` subtree respelled to this package, its ACP layer
rewritten as manifest contributions -- boards with the engine wave, together
with the ``raven.plugins`` entry point that makes the distribution
discoverable.
"""
