"""The context window a turn's model calls see, and how it is kept within bounds.

``compaction`` is the trigger arithmetic and the head-summary rendering,
``images`` the provenance and withdrawal notes pictures carry, ``shrink`` the
operations that make a transcript fit again and the per-turn bounds on them.
Cargo, like the harness: the loop imports this package and it imports nothing
of the loop, which is what lets the Memory role own the same decisions.
"""
