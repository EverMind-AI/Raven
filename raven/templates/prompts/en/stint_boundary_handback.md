## This is not finished yet

{{findings}}

Put that work back inside your own paths, then finish the task you were given.

This is not a command's output and no check has failed: it is the difference
between the tree when you started and the tree when you stopped, compared
against the paths named as yours. There is nothing to debug in the tooling --
the write happened, and it was undone. If the only way to do your task is to
change a file that is not yours, say so in your own output and leave the file
alone; the next round reads what you say.
