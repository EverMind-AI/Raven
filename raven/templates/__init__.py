"""Workspace template files (AGENTS.md, HEARTBEAT.md, SOUL.md, TOOLS.md, USER.md and
``memory/``) that ``raven.utils.workspace`` copies into a new workspace, plus the
``agents_scaffold/`` tree that ``raven agents new`` instantiates into a fresh
agent folder. The scaffold's ``.py`` files are data to be copied out, never
imported here -- the tree deliberately has no ``__init__.py`` of its own.
"""
