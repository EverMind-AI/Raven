# Raven-X 改动说明(raven-x 分支)

> 本分支 = Raven v0.1.9(`442f2fa`)+ 8 个改动提交。目的:依据 SWE-bench Verified 四框架
> (raven / hermes / codex / claude-code)× 500 题全量评测的逐题轨迹分析,修掉 raven 作为
> coding agent 的框架级缺陷。所有改动都是通用纪律,不含任何针对特定评测题目的内容。
> 本文档给评审者(人或 agent)读;评测口径、逐题台账在评测仓库,不在本仓。

## 成绩(SWE-bench Verified 136 题迭代子集,Qwen3.6-35B,temperature 1.0,pass@1)

| | 原版 raven v0.1.9 | 本分支(全部改动) | hermes 同口径对照 |
|---|---|---|---|
| 解出 | 57 | **71** | 74 |

同题重跑约 26% 概率翻面,单题对错不能直接归因;每轮改动都配了逐题轨迹归因与
"护栏零反作用"验证(BROKE 题逐题查轨迹,无一例由新护栏造成)。

## 改动一:运行环境泄漏修复(P0;**上游 main 同样存在,建议修复**)

**现象**:raven 在容器里替用户跑子进程时,子进程的 Python 环境被 raven 自带的依赖污染,
被测项目的老解释器(3.6/3.8)import 到 raven 的 3.12 版包 → ImportError/SyntaxError。
在 SWE 评测中表现为"连仓库自己的测试都跑不起来",是逐题诊断出的头号失败根因(47/60)。

**机制**:`--target site/` 式 bundle 依赖 `PYTHONPATH` 启动 raven;DirectExecutor 的环境
白名单刻意保留 PYTHONPATH 传给 exec 子进程;两者叠加把 raven 的依赖注入所有子进程。

**修复(两道保险)**:
- 部署约定:bundle 改 site-install 语义(依赖装进随附解释器自身 site-packages,启动不需要 PYTHONPATH);
- `raven/__main__.py`:启动时把 raven 自身安装目录从 PYTHONPATH 中剔除(防 legacy 启动方式)。

**效果**:修复后四轮 500+ 条轨迹污染特征零出现。

## 改动二:测试证据护栏(`raven/agent/loop/main.py`,评测态 opt-in)

设计总纲(调研 hermes / claude-code / codex / opencode 四家护栏 + 回放 408 条历史轨迹后定):
**框架只陈述可核实的事实,裁决权留给模型**;所有提醒整轮最多一次;测试输出判不出红绿就
什么都不触发;文档与 /tmp 临时文件豁免。三个环境变量开关,默认全关,不影响产品行为:

### 2.1 `RAVEN_REQUIRE_REAL_TEST_EVIDENCE=1` — 完成闸门
主循环监听 exec 命令+输出,同时命中"真实测试运行器"(pytest / runtests / bin/test /
unittest / tox / sympy.test)与"测试输出特征"才记为证据;模型宣布完成而全程无证据 →
注入引导消息拦回,封顶 2 次。依据:原版头号死因是"只用自写脚本验证就宣布完成";
纯提示词的 codex 在抽查中同样出现零验证收尾。效果:该类失败四轮清零。

### 2.2 `RAVEN_GATE_STALE=1` — 证据过期提醒(单发)
台账追踪"最近一次真实测试之后是否又改了非文档、非临时目录的文件";模型在该状态下宣布
完成 → 单发一条事实提醒(列出改动文件,建议重跑;若确定不影响行为可直接收尾)。

### 2.3 `RAVEN_GATE_RED=1` — 红灯质询(单发,双出口)
模型在"最近一次测试仍有失败、且之后没再改码"状态下宣布完成 → 单发质询,两条出口并列:
1. 失败测试断言的正是任务要求改掉的旧行为 → 测试过时:在回复里一句话说明哪条需求与它
   矛盾,保留修复,可以带着失败收尾;禁止**为了凑绿**削弱修复或改测试断言(禁动机,
   不禁修补丁——因补丁真有问题而修补丁正是出口 2 要求的动作);
2. 其他情况 → 失败指向补丁的真问题:修补丁重跑;修后仍失败要在收尾时明说。

**为什么不做"红灯必须修绿"的硬闸门**:回放 408 条历史轨迹发现 10 次"带着失败收尾、
判分却正确"(仓库可见测试停留在旧需求上)——硬拦会逼模型改错。hermes 的硬闸门要求
退出码 0,在这类局面无出口,只能靠拦满次数上限放行。实测本方案:"红着收尾却判对"的
反作用探针 8/8 存活;全量 19 次质询,判定全部有据,零测试断言篡改、零死循环、零补丁削弱。

### 2.4 配套工程修复
- 测试输出识别:匹配前剥离 ANSI 颜色码(颜色码粘在数字前使词边界匹配失效);识别 sympy
  模块式调用(`runtests` 不带 .py、`sympy.doctest`)及其输出报头;
- 会话持久化的大工具结果截断从"只留头部 16k"改为"头 12k + 尾 4k"——测试运行器把
  通过/失败总结打印在输出末尾,旧截断切掉的恰好是后续轮次最需要的证据。

## 改动三:工程纪律进身份提示词(`raven/context_engine/segments/render.py`)

在 identity 段新增 "Software Engineering Discipline" 块(Understand / Implement / Verify /
Before declaring done 四阶段),要点:根因修复而非症状修补;需求描述是行为意图的事实源,
断言"任务明确要改掉的旧行为"的测试是过时测试(保留正确修复),其余新挂的测试是自己
引入的回归(必须收窄补丁);每个修复点最小改动、同类缺陷全部修;证据分级——项目既有
测试优先,项目没有覆盖时按项目惯例新写正式测试并用真实运行器跑,随手自写检查是最弱
证据(它与改动共享同一套理解假设);收尾前重跑相关测试并通读完整 diff。
文案经过独立 agent 理解测试并按结论修订(消除条款间的矛盾裁决)。

## 提交索引(本分支,自 v0.1.9 起)

| commit | 内容 |
|---|---|
| `1140b19` | PYTHONPATH 泄漏修复 + 完成闸门(2.1) |
| `f3d790e` → `e1c14e2` | 工程纪律身份提示词(v1 → v3,经理解测试修订) |
| `841e69b` | 证据台账 + stale/red 单发触发器(2.2/2.3)+ 截断留头尾 |
| `261b3c3` | 测试输出识别修复(ANSI/sympy/临时目录豁免/禁装包文案) |
| `5565559` | Verify 段改证据分级(修"自写检查不算验证"在无测试项目中的死路表述) |

## 边界与已知事项

- 评测接线(任务提示词、判分)在评测框架仓库,不在本仓;评测框架侧另修了一个判分缺陷
  (标准测试补丁只新增文件时判分脚本误重置镜像状态,对全部 harness 一视同仁地修正)。
- 未做:定位辅助、上下文压缩、token 记账等(后续迭代方向)。
- 剩余与 hermes 的差距(71 vs 74)不在测试纪律层:失败主体是"可见测试全绿、补丁本身
  没把需求做全",需要内容能力层面的改进。
