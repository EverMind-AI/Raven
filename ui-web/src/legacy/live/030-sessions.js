/* ---- session list ------------------------------------------------ */
/* The shaping -- what a listed session looks like as a row, the stamp on it,
   the scheduler wrapper the title has to see past -- is the rail's own data
   now (ui-web/src/features/rail/source.ts), and the clock that moves a row is
   the registry's (ui-web/src/state/session/registry.ts). Nothing is left for
   this part to install. */

/* Everything this part used to do while the concatenated page script ran, in
   the same order. src/legacy/index.js is the only caller. */
export function install() {

}
